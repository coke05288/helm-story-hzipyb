apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: gateway-service  