apiVersion: v1
kind: Service
metadata:
  name: eureka-service
  namespace: {{ $.Values.global.namespace }}
spec:
  ports:
    - port: {{ $.Values.eureka-service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: eureka-service